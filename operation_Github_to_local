Transfer files/directories from Github remote repository to local:

1. Grab a complete directory:
git clone https://username:password@github.com/username/repository.git

2. Retrieve new files without merging into your local branch:
git fetch remotename

3. Merge changes into your local branch:
git merge remotename/branchname

4. Complete fetch and merge at the same time:
git pull remotename branchname


Checkout new stuff from Github to local:
git pull origin master


Ignore all local changes and sync with Github (dangerous!)
git fetch --all
git reset --hard origin/master


Download a single file to local machine:

Right click on the "raw" button and save it as a file
or
Click the "raw" button and "wget URL"
